from django.apps import AppConfig

class PortailAdminConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.portail_admin"
    verbose_name = "Portail Administrateur"
